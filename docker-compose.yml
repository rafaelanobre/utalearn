services:
  backend:
    build: backend/
    ports:
      - "8000:8000"
    volumes:
      - ./localdata:/data:ro
      - ./backend:/app
    environment:
      - DATA_DIR=/data
    command: uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload
